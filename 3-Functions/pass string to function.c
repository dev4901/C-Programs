// CPP program to illustrate strstr() 
//Aim - if the substring s2 is found in string s1 then that substring is replaced by the word "strings"
#include <string.h> 
#include <stdio.h> 

void print(char str[]);

int main() 
{ 
	// Take any two strings 
	char s1[] = {"sijifgjigsgkjfg"}; 
	print(s1);
} 

void print(char str[])
{
	printf("%s\n",str);
}
