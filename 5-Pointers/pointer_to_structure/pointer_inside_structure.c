#include<stdio.h>
#include<stdlib.h>

struct abc{
	int *x;
	char *str;
};

int main()
{
	/*struct abc myvar;
	myvar.x=(int*)calloc(1,sizeof(int));
	myvar.str=(char*)malloc(20*sizeof(char));
	*(myvar.x)=25;
	printf("enter the string value - ");
	scanf("%s",myvar.str);
	printf("string is %s \ninteger is %d",(myvar.str),*(myvar.x)); */
	
}
